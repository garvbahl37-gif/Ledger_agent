# Where the analysis engine lives.
#
# The interface is a static bundle; the agents, sandbox and statistics run in
# the Python service under ../backend. Set this to that service's URL when the
# two are not on the same machine. Defaults to http://localhost:8000.
VITE_API_BASE=http://localhost:8000
